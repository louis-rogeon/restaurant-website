<!DOCTYPE html>
<html lang="fr">
  <head>
    <% include ../partials/head %>
  </head>


  <body>

    <% include ../partials/header %>

    <!-- Begin page content -->
    <div class="container test">
      <div class="page-header row">
        <h1>Réserver une table</h1>
      </div>
      <%if(typeof message_alert != 'undefined') {%>
        <p class="alert">
        <%-message_alert%>
        </p>
      <%}%>
      <div class="reservation row">
          <form class="col-lg-12" id="form-resa" method="post" action="/reserver">
            <legend>Complétez le formulaire pour réserver votre table</legend>
            <div class="form-group">
              <label for="nom">Votre nom</label>
              <input id="nom" name="nom" type="text" class="form-control" autofocus>
            </div>
            <div class="form-group">
              <label for="nbrInvit">Nombre de personnes présentes</label>
              <input id="nbrInvit" name="nbrInvit" type="number" min="1" max="15" class="form-control">
            </div>
            <div class="form-group">
              <label for="mail">Adresse mail</label>
              <input id="mail" name="mail" type="email" class="form-control">
            </div>
            <div class="form-group">
              <label for="tel">Numéro de téléphone (le numéro servira à l'équipe du restaurant pour vous appellez en cas d'annulation)</label>
              <input id="tel" name="tel" type="tel" class="form-control">
            </div>
            <div class="form-group">
              <label for="date">Date</label>
              <input id="date" name="date" type="date" class="form-control">
            </div>
            <div class="form-group">
              <label for="heure">Heure</label>
              <input id="heure" name="heure" type="time" class="form-control">
            </div>
            <button type="submit"><span class="glyphicon glyphicon-ok-sign"></span> Réserver</button>
            <!--input type="submit" id="ajout_com" value="Réserver"-->
          </form>
      </div>
    </div>

    <% include ../partials/footer %>

  </body>
</html>
